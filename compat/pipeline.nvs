// FROZEN COMPAT FIXTURE — do not modify to match new behavior.
// Exercises 2.1.0-era pipelines (|>).
fn double(x) { return x * 2 }
fn add1(x) { return x + 1 }
print 5 |> double |> add1
print [1, 2, 3] |> len
