[package]
name = "nvs"
version = "0.1.0"
edition = "2021"
description = "Safe Rust bindings for the NvS (Navescript) interpreter via its C ABI"
license = "MIT"

[lib]
name = "nvs"
crate-type = ["rlib"]

[dependencies]
serde_json = "1"

[dev-dependencies]
